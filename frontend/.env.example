# URL pública do backend. Em dev local, aponta para o backend rodando na
# máquina; em produção (Vercel), configurar como env var no painel do projeto
# apontando para a URL do backend no Render (ex.: https://cmms-backend.onrender.com).
VITE_API_URL=http://localhost:3333
